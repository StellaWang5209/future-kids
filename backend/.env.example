# Future Kids backend (all optional)
# PostgreSQL mirror — leave empty to run metadata-only
DATABASE_URL=
# IPFS
IPFS_GATEWAY=https://ipfs.io/ipfs/
BASE_TOKEN_URI=ipfs://future-kids-metadata/
PORT=4000
